export enum WidgetType {
    ADS,
    CNT
}

export enum LayoutType {
    CAR,
    HOR,
    HRO,
    BAN,
    MRC
}

export enum ImageType {
    LDS,
    PR1,
    PR2,
    SQR,
    CIR,
    CST,
    LDS2
}

export enum ListingLayoutType {
    GRD,
    LST
}

export enum ImageSource {
    MNL,
    AST
}

export enum HeroLandingPage {
    AST,
    DEF,
    PDF,
    PLT,
    HTM
}

export enum PreDefinedLandingTarget {
    LGN,
    IFP,
    SRH,
    RCG,
    CLT,
    CDT,
    QUZ
}

export enum CarouselIndicators {
    TOP,
    BOT,
    HDN
}

export enum ImageTypeNames {
    CIRCLE,
    CUSTOM,
    LANDSCAPE_16_9,
    LANDSCAPE_120_37,
    PORTRAIT_9_16,
    PORTRAIT_2_3,
    SQUARE,
}


export enum RailConstant {
    CON_W,
    MY_W
}

export enum RailType {
    K_PDF
}